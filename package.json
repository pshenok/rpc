{
  "name": "rpc",
  "version": "1.0.0",
  "description": "JSON-RPC Client",
  "main": "lib/",
  "types": "lib/",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:w": "tsc -w -p tsconfig.json",
    "clear": "rm -rf dist",
    "test:unit": "jest --config jest.config.json",
    "test:w": "jest --watch",
    "coverage": "jest --coverage",
    "lint": "eslint --quiet --ext .ts .",
    "lint:fix": "eslint --fix --ext .ts ."
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com:pshenok/rpc.git"
  },
  "author": "pshenok",
  "license": "UNLICENSED",
  "private": true,
  "eslintIgnore": [
    "lib/**/*.js",
    "lib/**/*.d.ts"
  ],
  "dependencies": {
    "jayson": "^3.0.2",
    "json-stringify-safe": "^5.0.1"
  },
  "devDependencies": {
    "@types/json-stringify-safe": "^5.0.0",
    "@typescript-eslint/eslint-plugin": "^2.1.0",
    "@typescript-eslint/parser": "^2.1.0",
    "dotenv": "^8.0.0",
    "eslint": "^6.3.0",
    "husky": "^3.0.7",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.2.0",
    "typescript": "^3.4.5"
  }
}
